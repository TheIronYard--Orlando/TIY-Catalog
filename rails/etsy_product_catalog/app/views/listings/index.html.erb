<h1>Etsy </h1>

  <div class="signup">
    <%= link_to 'Login', verify_path %>
  </div>

  <% @listings.each do |list| %>
    <ul>
      <li><%= link_to list.title, listing_path(list.id) %></li>
     </ul> 
  <% end %>