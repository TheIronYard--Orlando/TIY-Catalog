<h1>Product details</h1>

    <h2><%= @listing.title %></h2>
    <div><%= image_tag("#{@listing.main_image}", size: "470x470") %></div> 
    <p><%= @listing.description %></p>

    <div class="to_the_right">
      <h3>Price: $<%= @listing.price %>0</h3>

      <%= link_to 'Back', root_path, class: 'back_button' %>
    </div>
